---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: kea-debian9-memfile
    image: debian:stretch
    env:
      container: docker
    groups:
      - deploy
  - name: kea-debian9-mysql
    image: debian:stretch
    env:
      container: docker
    groups:
      - deploy
      - mysql

provisioner:
  name: ansible
  inventory:
    links:
      group_vars: group_vars/
      host_vars: host_vars/
  playbooks:
    converge: ../ressources/playbook.yml
  options:
    vvv: true

scenario:
  name: deploy-debian-9
  test_sequence:
    - destroy
    - dependency
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - side_effect
    - verify
    - destroy

verifier:
  name: testinfra
  lint:
    name: flake8
