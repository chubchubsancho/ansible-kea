---
- name: "install epel-release"
  yum:
    name: epel-release
    state: present
  tags:
    - install
    - prerequisites

- name: "install building requirements"
  yum:
    name: "{{ kea_building_requirements }}"
    state: present
  tags:
    - install
    - prerequisites

# - name: "install centos building requirements"
#   yum:
#     name: "{{ kea_building_requirements_centos }}"
#     state: present
#   tags:
#     - install
#     - prerequisites
